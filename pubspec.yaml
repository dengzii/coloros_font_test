name: coloros_font_issues
description: "ColorOS Font Test."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  system_fonts: ^1.0.1

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true